import networkx as nx
from collections import defaultdict
import pickle

def loseRegion(g, W, action1, action2):
    """
    :param g: graph g
    :param W: lose region W
    :param action1: agent1 connection
    :param action2: agent2 connection
    :return: the result dict
    """
    for node in W:

    return res
